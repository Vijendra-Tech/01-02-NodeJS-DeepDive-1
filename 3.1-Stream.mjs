const stream1; //readable stream
const stream2 //writable stream

//Stream pattern 
// const resultStream  = readableStream.pipe(writeableStream).pipe(Writeable2).pipe(final)
 
// pipe return a readbale stream  at the end.



